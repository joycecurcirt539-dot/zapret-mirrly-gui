<Page
    x:Class="ZapretMirrlyGUI.Pages.ListsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:ZapretMirrlyGUI.Pages"
    mc:Ignorable="d"
    NavigationCacheMode="Required">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Left Column: Master List Metadata Sidebar -->
        <Grid Grid.Column="0" x:Name="ListsSidebarGrid" Background="Transparent" BorderThickness="0" Padding="12,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Sidebar Title -->
            <StackPanel Grid.Row="0" Margin="4,24,4,12" Spacing="2">
                <TextBlock Text="Списки доменов" Style="{StaticResource MirrlyTextLargeStyle}" FontSize="15" FontWeight="Bold" />
                <TextBlock Text="Настройка файлов правил DPI" Style="{StaticResource MirrlyTextSmallStyle}" FontSize="11" Opacity="0.6" />
            </StackPanel>

            <!-- Sidebar Metadata ListView -->
            <ListView Grid.Row="1" x:Name="ListsSidebarListView" ItemsSource="{x:Bind ViewModel.ListMetadata, Mode=OneWay}" SelectedItem="{x:Bind ViewModel.ActiveItem, Mode=TwoWay}" SelectionMode="Single">
                <ListView.Resources>
                    <ResourceDictionary>
                        <ResourceDictionary.ThemeDictionaries>
                            <ResourceDictionary x:Key="Dark">
                                <SolidColorBrush x:Key="ListViewItemBackgroundPointerOver" Color="#12FFFFFF" />
                                <SolidColorBrush x:Key="ListViewItemBackgroundSelected" Color="#1CFFFFFF" />
                                <SolidColorBrush x:Key="ListViewItemBackgroundSelectedPointerOver" Color="#22FFFFFF" />
                                <SolidColorBrush x:Key="ListViewItemBackgroundPressed" Color="#28FFFFFF" />
                                <SolidColorBrush x:Key="ListViewItemForegroundSelected" Color="#00F5D4" />
                                <SolidColorBrush x:Key="ListViewItemForegroundSelectedPointerOver" Color="#00F5D4" />
                                <SolidColorBrush x:Key="ListsSidebarTextForeground" Color="#F4F4F5" />
                            </ResourceDictionary>
                            <ResourceDictionary x:Key="Light">
                                <SolidColorBrush x:Key="ListViewItemBackgroundPointerOver" Color="#08000000" />
                                <SolidColorBrush x:Key="ListViewItemBackgroundSelected" Color="#12000000" />
                                <SolidColorBrush x:Key="ListViewItemBackgroundSelectedPointerOver" Color="#18000000" />
                                <SolidColorBrush x:Key="ListViewItemBackgroundPressed" Color="#1C000000" />
                                <SolidColorBrush x:Key="ListViewItemForegroundSelected" Color="#8B7E66" />
                                <SolidColorBrush x:Key="ListViewItemForegroundSelectedPointerOver" Color="#8B7E66" />
                                <SolidColorBrush x:Key="ListsSidebarTextForeground" Color="#1C1C1E" />
                            </ResourceDictionary>
                        </ResourceDictionary.ThemeDictionaries>
                        
                        <CornerRadius x:Key="ListViewItemCornerRadius">6</CornerRadius>
                        <local:StringToGeometryConverter x:Key="StringToGeoConverter" />
                    </ResourceDictionary>
                </ListView.Resources>
                
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Padding" Value="12,10,12,10" />
                        <Setter Property="CornerRadius" Value="6" />
                        <Setter Property="Margin" Value="0,2,0,2" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListView.ItemContainerStyle>

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid ColumnDefinitions="Auto,*">
                            <Viewbox Grid.Column="0" Width="16" Height="16" Margin="0,0,12,0" VerticalAlignment="Center">
                                <Path Data="{Binding IconData, Converter={StaticResource StringToGeoConverter}}" Fill="#8A8A8F" Stretch="Uniform" />
                            </Viewbox>
                            <StackPanel Grid.Column="1" Spacing="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding DisplayName}"
                                           FontSize="13" FontWeight="SemiBold"
                                           Foreground="{ThemeResource ListsSidebarTextForeground}" />
                                <TextBlock Text="{Binding FileName}"
                                           FontSize="10.5" Opacity="0.5"
                                           Foreground="#8A8A8F" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <!-- Right Column: Editor Workspace -->
        <Grid Grid.Column="1" x:Name="ListsEditorGrid" Padding="{StaticResource PagePadding}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- 1. Header & Actions Area -->
            <Grid Grid.Row="0" Margin="0,40,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Title Details -->
                <StackPanel Grid.Column="0" Spacing="4" Margin="0,0,16,0">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock x:Name="ActiveListTitleTextBlock" Text="{x:Bind ViewModel.ActiveFileName, Mode=OneWay}" Style="{StaticResource MirrlyTextLargeStyle}" Foreground="{ThemeResource BrandElectricBrush}" FontWeight="Bold" />
                    </StackPanel>
                    <TextBlock x:Name="ActiveListSubtitleTextBlock" Text="{x:Bind ViewModel.ActiveDescription, Mode=OneWay}" Style="{StaticResource MirrlyTextSmallStyle}" Opacity="0.8" TextWrapping="Wrap" />
                </StackPanel>
            </Grid>

            <!-- 2. Central Editor Frame -->
            <Border Grid.Row="1" Style="{StaticResource GlassCardStyle}" Padding="4">
                <TextBox x:Name="ListEditorTextBox" Text="{x:Bind ViewModel.EditorText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="NoWrap" FontFamily="Consolas" FontSize="13" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Auto" Background="Transparent" BorderThickness="0" />
            </Border>

            <!-- 3. Save & Revert Buttons -->
            <Grid Grid.Row="2" Margin="0,16,0,0" ColumnDefinitions="*,Auto,Auto">
                <TextBlock Grid.Column="0" x:Name="ListSaveStatusTextBlock" Text="{x:Bind ViewModel.StatusInfo, Mode=OneWay}" Style="{StaticResource MirrlyTextSmallStyle}" VerticalAlignment="Center" Foreground="{ThemeResource BrandElectricBrush}" />

                <Button Grid.Column="1" x:Name="RevertButton" Command="{x:Bind ViewModel.ResetCurrentFileCommand}" Margin="0,0,8,0" Height="36">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="Сбросить" />
                    </StackPanel>
                </Button>

                <Button Grid.Column="2" x:Name="SaveChangesButton" Command="{x:Bind ViewModel.SaveCurrentFileCommand}" Style="{StaticResource PremiumAccentButtonStyle}" Height="36">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="Сохранить список" />
                    </StackPanel>
                </Button>
            </Grid>
        </Grid>
    </Grid>
</Page>
