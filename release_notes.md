## Zapret Mirrly GUI v1.0.3

Релиз с обновлением встроенной базы zapret до актуальной версии и восстановлением статуса официальных репозиториев Flowseal.

### Изменения и улучшения
- **Обновление zapret**: Встроенная утилита обхода DPI от Flowseal обновлена до актуальной версии **1.9.9d**.
- **Разблокировка аккаунта Flowseal**: Из интерфейса приложения (раздел «О программе» / Поддержка) и документации удалены предупреждения о временной блокировке профиля Flowseal, так как его аккаунт на GitHub был успешно разморожен.
- **Добавление зеркал SourceForge**: Добавлены альтернативные ссылки на официальные зеркала проектов Flowseal на SourceForge в интерфейсе приложения и в файле `README.md`:
  - [zapret-discord-youtube на SourceForge](https://sourceforge.net/projects/zapret-discord-youtube/)
  - [tg-ws-proxy на SourceForge](https://sourceforge.net/projects/tg-ws-proxy/)
- **Адаптация скриптов**: Оригинальный скрипт диагностики `test zapret.ps1` версии 1.9.9d полностью адаптирован для работы внутри графической оболочки (подавлено блокирующее чтение нажатия клавиш `ReadKey` в фоновом GUI-режиме).

---

## Zapret Mirrly GUI v1.0.2 (Experiment)

Экспериментальный релиз с параллельной интеграцией **TgWsProxy** для обхода блокировок Telegram на ПК.

### Изменения и нововведения
- **Интеграция TgWsProxy (Эксперимент)**:
  - Добавлена новая вкладка управления «Telegram Proxy» в боковом меню приложения.
  - Реализован запуск/остановка локального прокси-сервера TgWsProxy в фоновом режиме параллельно со службой Zapret. Поскольку они используют разные механизмы и порты (Zapret работает через драйвер WinDivert, а TgWsProxy — как локальный SOCKS5/MTProto прокси на порту 1443), конфликты полностью исключены.
  - Добавлены настройки интерфейса прокси (порт, хост, MTProto-секрет, переключатель Cloudflare Worker).
  - Внедрена кнопка **«Подключить Telegram»**, автоматически открывающая ссылку конфигурации `tg://proxy?server=...` для мгновенной привязки к клиенту Telegram.
  - Реализована трансляция логов `TgWsProxy` в реальном времени с автопрокруткой.
  - Настроено автоматическое закрытие фонового процесса прокси при выходе из GUI или закрытии приложения.
- **Ограничения и статус эксперимента**:
  - В связи с временной блокировкой оригинального репозитория разработчика Flowseal на GitHub (отсутствие доступа к актуальному исходному коду `tg-ws-proxy`), в приложении используется оригинальный скомпилированный исполняемый файл `TgWsProxy_windows.exe` в качестве временного решения.
  - В будущем планируется переписать всю логику транспортного туннеля непосредственно на чистый сетевой код C# внутри приложения. Это позволит полностью избавиться от внешнего исполняемого файла, снизить вес сборки и исключить ложные срабатывания Windows Defender на сторонний софт.

---

## Zapret Mirrly GUI v1.0.1

Релиз стабильности и оптимизации интерфейса. 

### Изменения и исправления
- **Устранение падения скрипта диагностики**: Скрипт тестирования стратегий (`test zapret.ps1`) теперь работает в неинтерактивном режиме с подавлением всех консольных чтений ввода (`ReadKey` / `Read-Host`) при запуске из GUI. Добавлено глобальное логирование ошибок диагностики.
- **Интеграция ServiceController**: Управление статусом службы переведено с парсинга консольного вывода `sc.exe` на прямое API ServiceController. Устранены ошибки отображения статуса службы в состоянии `UNKNOWN`.
- **Новый цветовой язык логов**: Текстовые логи заменены на RichTextBlock с цветовым кодированием (5 категорий: ошибки логов — красные, предупреждения — оранжевые, успехи — зеленые, команды — синие, общая информация — серые). Добавлены красивые XAML-подсказки с цветовой схемой.
- **Защита от двойных кликов**: Добавлены асинхронные индикаторы загрузки (ProgressRing) и автоматическое отключение интерактивности элементов управления во время выполнения операций.
- **Оптимизация веса сборки**: Исключены временные и тестовые директории публикации из метаданных проекта, что уменьшило размер итогового исполняемого файла с 611 МБ до стабильных ~307 МБ.

---

## Zapret Mirrly GUI v1.0.0

Первый публичный релиз — GUI для утилиты обхода DPI, построенный на сборке **Flowseal** ([zapret-discord-youtube](https://github.com/Flowseal/zapret-discord-youtube)) и оригинальном движке **bol-van** ([zapret](https://github.com/bol-van/zapret)).

---

### Возможности
- Управление Windows-службой `winws` в один клик — запуск, остановка, автозапуск
- Пресеты обхода DPI: FAKE TLS, SIMPLE FAKE, ALT и другие — для YouTube и Discord
- Встроенная диагностика доступности Discord и YouTube (DNS, Ping, HTTP)
- Живой лог `winws.exe` в реальном времени
- Редактор списков доменов для тонкой настройки обхода
- Современный WinUI 3 интерфейс с тёмной темой и Fluent-анимациями

---

### Важно
- Требуются **права Администратора** (для работы драйвера WinDivert)
- Обход Telegram **не гарантируется** — для Telegram используй [tg-ws-proxy](https://github.com/Flowseal/tg-ws-proxy) от Flowseal
- Скачивай только из официального репозитория, не из зеркал

### Установка
Скачай `ZapretMirrlyGUI.exe` ниже и запусти от имени Администратора. Установка не нужна — всё в одном файле.

---

### Легенды, без которых это невозможно
- **[Flowseal](https://github.com/Flowseal)** — создатель `zapret-discord-youtube` и `tg-ws-proxy`
- **[bol-van](https://github.com/bol-van)** — создатель оригинального движка `zapret` и `winws`
